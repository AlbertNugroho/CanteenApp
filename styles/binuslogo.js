import { StyleSheet } from "react-native";

export const binuslogo = StyleSheet.create({
  binus: {
    color: "white",
    fontSize: 44,
    textAlign: "center",
    fontWeight: "bold",
  },
  canteen: {
    color: "#ECB819",
    fontSize: 32,
    fontWeight: "bold",
  },
});

export default binuslogo;
